name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-and-release:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: system_monitor-linux-amd64
            asset_name: system_monitor-linux-amd64
          
          - os: macos-latest
            artifact_name: system_monitor-macos-arm64
            asset_name: system_monitor-macos-arm64
          
          - os: windows-latest
            artifact_name: system_monitor-windows-amd64.exe
            asset_name: system_monitor-windows-amd64.exe
    
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure CMake
        run: cmake -B build -DCMAKE_BUILD_TYPE=Release

      - name: Build
        run: cmake --build build --config Release
      
      - name: Upload Release Asset
        uses: softprops/action-gh-release@v2
        with:
          files: |
            build/**/system_monitor*
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}